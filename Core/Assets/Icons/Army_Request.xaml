<UserControl x:Class="WpfAppMilitaryExport.Army_Request"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             TextElement.Foreground="Gray"
             TextElement.FontWeight="Regular"
             xmlns:local="clr-namespace:WpfAppMilitaryExport"
             mc:Ignorable="d"
            
             d:DesignHeight="400" d:DesignWidth="800">
    <UserControl.Resources>
        <LinearGradientBrush x:Key="PageBackground" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#273046" Offset="0"/>
            <GradientStop Color="#131b28" Offset="1"/>
        </LinearGradientBrush>
        <Style x:Key="MenuItemStyle" TargetType="MenuItem">
            <Setter Property="Foreground" Value="CadetBlue" />
        </Style>
    </UserControl.Resources>

    
    <Grid>
       
        <Grid.Background>
            <StaticResource ResourceKey="PageBackground"/>
        </Grid.Background>
        <Viewbox Width="auto" Height="auto" Stretch="Uniform">
         <Grid Margin="0,90,0,0">
                <Image  Source="../Image/MainImages/ВМС.png" Width="200" Height="250" HorizontalAlignment="Left" Margin="0,0,0,0"/>
             <Image  Source="../Image/MainImages/ВВС.png" Width="200" Height="250" HorizontalAlignment="Center" Margin="0,0,0,0"/>
                <Image  Source="../Image/MainImages/Сухопутні сили.png" Width="200" Height="250" HorizontalAlignment="Right" Margin="450,0,0,0"/>
        </Grid>
        </Viewbox>

       <Viewbox VerticalAlignment="Top" Width="auto" Height="auto" Stretch="Uniform">
         <Grid Margin="0,0,0,0" >
            <materialDesign:ColorZone Margin="0,0,0,6" Width="800" Height="60" Mode="Inverted">

                <Grid Margin="0,0,0,0">
                    <Image x:Name="image4" Source="../Image/MainImages/Command.png" Height="54" Width="86" HorizontalAlignment="Center" Margin="0,0,0,0"/>
                    <Menu HorizontalAlignment="left"  materialDesign:MenuAssist.TopLevelMenuItemHeight="25" Margin="20,15,0,0">

                        <MenuItem Foreground="LightSlateGray" Header="ВМС" Style="{StaticResource MenuItemStyle}">
                            <MenuItem Header="Деталі" Margin="10,0,0,0" Click="MenuNavyDetails_Click"/>
                            <MenuItem Header="Озброєння" Margin="10,0,0,0" Click="MenuNavyWeapon_Click"/>
                        </MenuItem>
                            <MenuItem Foreground="LightSlateGray" Margin="10,0,0,0" Header="ПЗС" Style="{StaticResource MenuItemStyle}">
                            <MenuItem Header="Літаки" Margin="10,0,0,0" Click="MenuAir_Click"  />
                            <MenuItem Header="Деталі" Margin="10,0,0,0" Click="MenuDetails_Click"/>
                            <MenuItem Header="Боєприпаси"  Margin="10,0,0,0" Click="MenuAmmo_Click"/>
                        </MenuItem>
                            <MenuItem Foreground="LightSlateGray" Margin="10,0,0,0" Header="Сухопутні Сили" Style="{StaticResource MenuItemStyle}">
                            <MenuItem Header="Піхотне спорядження" Margin="10,0,0,0" Click="MenuInfarny_Click" />
                            <MenuItem Header="Техніка" Margin="10,0,0,0" Click="MenuWeapon_Click"/>

                        </MenuItem>
                    </Menu>
                    <materialDesign:Card  HorizontalAlignment="Right" Margin="0,0,0,10" Height="40">
                        <Menu  materialDesign:MenuAssist.TopLevelMenuItemHeight="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type materialDesign:Card}}, Path=ActualHeight}"/>
                    </materialDesign:Card>

                    <materialDesign:Card Background="LightGray" HorizontalAlignment="Right" Height="40" Margin="0,0,20,0">
                        <Menu  materialDesign:MenuAssist.TopLevelMenuItemHeight="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type materialDesign:Card}}, Path=ActualHeight}">

                                <MenuItem   Foreground="LightSlateGray"  Header="Меню">
                                    <MenuItem Header="Вихід"  Click="Click_Exit"/>
                                    <MenuItem Header="Відправити запит до Міністерства оборони" Click="CreateQuery_Click"/>
                                </MenuItem>
                        </Menu>
                    </materialDesign:Card>
               </Grid>

            </materialDesign:ColorZone>
        </Grid>
       </Viewbox>

    </Grid>

</UserControl>